// found errors in the loop
#include<stdio.h>
int main()
{
	int i,j,k,a[20],s;
	printf("enter the no of elements\n");
	scanf("%d",k);
	printf("Enter the elements\n");
	for(i=0;i<k;i++) // increment instead of decrement
	{
		scanf("%d",&a[i]);
	}
	// bubble sorting

	// for(i=0;i<k-1;i++)
	// {
	// 	for(j=0;j<k-1-i;i++)




 

	// {
		for(l=5;j<l-1-i;l--) //We are decrementing instead of incrementing

		{
			if(a[l]>a[l+1])
			{
				s=a[l];
				a[l]=a[l+1];
				a[l+1]=s;
			}
		}
	} 
	printf("The elements in sorted array are:\n");
	for(i=0;i<k;++i)
	{
		printf("%d\n",a[i]);
	}
	
}
